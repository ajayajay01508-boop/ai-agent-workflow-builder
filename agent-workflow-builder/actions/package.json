{
  "name": "agent-workflow-builder-actions",
  "version": "1.0.0",
  "description": "Hasura Action handler for the AI Agent Workflow Builder",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1",
    "qs": "^6.16.0"
  }
}
